config:
  vars:
    - name: USER_NAME
      desc: Username to login into console and oc
      value: "dev"
    - name: USER_PASSWORD
      desc: Password to login
      value: "dev"
    - name: GIT_HOST
      desc: The git host prefix
      value: <url>
    - name: DOCS_URL
      desc: URL prefix to access docs (https://docs.spektrasystems.com/latest)
      value: <url>
      
modules:
  Lab01:
    name: Lab 1
    vars:
      USER_NAME:
  Lab02:
    name: Lab 2
    vars:
      GIT_HOST:
  Lab03:
    name: Lab 3
    vars:
      USER_NAME:
  Lab04:
    name: Lab 4
    vars:
      GIT_HOST:
  Lab05:
    name: Lab 5
    vars:
      USER_NAME:

      
